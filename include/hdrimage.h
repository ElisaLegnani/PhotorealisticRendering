#include "colors.h"

#ifndef _hdrimage_h_
#define _hdrimage_h_

class HdrImage {

public:
  int width, height;
  std::vector<Color> pixels;

  HdrImage(w,h){};

  ~HdrImage(){};
  
  pixel_offset(x,y){
    return
  }
};

#endif
